import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';
import Button from '../../Components/Button';

const Doctors = () => {
  return (
    <SafeAreaView>
      <Text>THis is the doctors</Text>
    </SafeAreaView>
  );
};

export default Doctors;
