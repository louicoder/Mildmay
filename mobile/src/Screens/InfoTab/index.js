import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

const InfoTab = () => {
  return (
    <SafeAreaView>
      <Text>This is the info tab</Text>
    </SafeAreaView>
  );
};

export default InfoTab;
